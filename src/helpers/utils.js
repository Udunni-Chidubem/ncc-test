require('dotenv').config()
const db = require('../models')
const User = db.rest.models.User

module.exports = {
    getFarmerProfile: (req, res, next) => {
        
    },
    getTraderProfile: (req, res, next) => {
        
    },
    getCompanyProfile: (req, res, next) => {
        
    }
}